#指定许可证服务的名称,以便Spring Cloud Config客户端知道正在查找哪个服务
#指定服务应该运行的默认profile oprofile映射到环境
#指定Spring Cloud Config服务器的位置
spring:
  application:
    name: licensingservice
  profiles:
    active: default
  cloud:
    config:
      uri: http://localhost:8888
server:
  port: 9090